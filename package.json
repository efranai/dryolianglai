{
  "name": "dryolianglai",
  "version": "1.0.0",
  "private": true,
  "description": "台中質子治療・放射治療｜賴宥良醫師",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "dist": "node scripts/dist.mjs",
    "deploy": "npm run build && npm run dist && wrangler pages deploy --commit-dirty=true && npm run indexnow",
    "indexnow": "node scripts/indexnow.mjs"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "qrcode": "^1.5.4"
  }
}
